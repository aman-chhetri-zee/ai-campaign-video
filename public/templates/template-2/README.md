# template-2

Second reference video for the POC pipeline. Source asset uploaded by the user.

The Stage 1 ingestion script (`scripts/ingest-templates.ts`) extracts `first_frame.png` and writes `metadata.json` with the motion script + style + pose archetypes used by orchestration.
