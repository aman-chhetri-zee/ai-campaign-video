# product-1

POC product fixture (converted from user-provided `product-1.jpeg`).

The Stage 2 ingestion script (`scripts/ingest-products.ts`) writes `metadata.json` with `product_type`, `attachment_strategy`, and `visual_description` used during keyframe compositing.
