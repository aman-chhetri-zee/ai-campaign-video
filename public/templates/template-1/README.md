# template-1

Reference video for the POC pipeline. Source asset uploaded by the user; see `video.mp4`.

The Stage 1 ingestion script (`scripts/ingest-templates.ts`) extracts `first_frame.png` and writes `metadata.json` with the motion script + composition notes used by the orchestration prompt.
