# template-higgs

AI-generated reference video from Higgsfield. Trimmed to 10s for Seedance/Kling motion-control compatibility.

If a source prompt is available, store it at `source_prompt.txt` in this directory — the orchestrator will use it as the motion_prompt instead of Gemini's reverse-engineered description.
